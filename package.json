{
  "name": "tyndalestutz.github.io",
  "private": true,
  "scripts": {
    "preview": "python3 -m http.server 8000 --bind 127.0.0.1",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
